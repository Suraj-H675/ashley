# Ashley loading screen

A phone-first loading screen built from the supplied cat sketch and paw artwork.

Open `index.html` directly, or serve this folder locally:

```sh
python -m http.server 8000
```

When the main application is ready, dismiss the loader with:

```js
window.ashleyLoader.hide();
```
